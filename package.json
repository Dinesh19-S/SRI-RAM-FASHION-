{
    "name": "sri-ram-fashion-app",
    "version": "1.0.0",
    "description": "Sri Ram Fashion App - Backend API",
    "scripts": {
        "start": "node backend/src/index.js",
        "build": "echo 'Backend ready' && npm install --prefix backend",
        "postinstall": "npm install --prefix backend"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}